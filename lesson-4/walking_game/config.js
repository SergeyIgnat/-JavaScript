/**
 * Объект с настройками игры
 * @property {int} rowsCount - количество строк в карте
 * @property {int} colsCount - количесвто рядов в карте
 */
const config = {
    rowsCount: 10,
    colsCount: 10,
}
